#The path "security.firewalls.stub.auth0_login.check_path" cannot contain an empty value, but got null.

security:
    providers:
        stub:
            id: foo
        auth0_in_memory:
            auth0_memory:
                user_data_loader:
                    default:
                        identifier: email
                        auth0_key: sub
                        map_options:
                            sub: auth0_user_key
                            picture: avatar

    firewalls:
        stub:
            provider: auth0_in_memory
            auth0_login:
                check_path:
                user_data_loader:
                    default:
                        map_options:
                            key1: value1
                            key2: value2
                            key3: value3
